{
  "name": "lambda-bun-poc",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun index.ts",
    "clean": "rm -rf dist && echo 'Done.'",
    "dev": "bun --hot index.ts",
    "dev:watch": "bun --watch index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
